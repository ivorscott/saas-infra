apiVersion: apps/v1
kind: Deployment
metadata:
  name: mic-app
spec:
  selector:
    matchLabels:
      app:efd36f2
  template:
    metadata:
      labels:
        app:efd36f2
    spec:
      imagePullSecrets:
        - name: ghcr-auth
      containers:
        - image: ghcr.io/devpies/app:efd36f2
          name: mic-app
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: mic-app-svc
spec:
  selector:
    app:efd36f2
  ports:
    - port: 80
      targetPort: 80